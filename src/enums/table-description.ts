export enum Table {
    USER = 'user'
}

export enum UserColumn {
    ID = "id",
    USERNAME = 'username',
    EMAIL = 'email',
    FIRST_NAME = 'firstname',
    LAST_NAME = 'lastname',
    PASSWORD = 'password'
}

export type Column = UserColumn;
